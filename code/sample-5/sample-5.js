const fetch = require('isomorphic-fetch');
/**
 * Let us get some images from https://picsum.photos/list!
 */
const getSomeImages = function getSomeImages(count, callback) {}

module.exports = getSomeImages;
